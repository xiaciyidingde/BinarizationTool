# 更新日志

## [v1.5.6.0] - 2026-03-28

### 主要：完善图层系统

#### 新增功能
- **用户图层视图系统**：用户图层支持原图/预处理/二值化三种视图模式
  - 原图模式：显示图层对应的原图区域
  - 预处理模式：显示应用预处理参数后的效果
  - 二值化模式：显示图层的二值化结果
  - 未选中区域统一显示为灰色背景

#### 修复
- 修复视图系统所有模式都显示二值化结果的问题
- 修复切换图层时UI显示错误参数的问题（参数结构不匹配）
- 修复用户图层与根图层内容不一致的问题
- 修复用户图层在预处理视图中修改参数后切换到二值化视图的问题
- 修复用户图层二值化视图显示逻辑错误（只覆盖黑色像素）
- 修复撤销/重做后根图层消失的问题
- 修复撤销/重做不更新图层面板UI的问题
- 修复切换图层时选区消失但数据保留的问题
- 修复裁切后图层索引错误

#### 优化
- 优化选区边框渲染：使用信号监听代替固定延迟，适应不同设备性能
- 优化信号连接：在初始化时连接一次，避免重复连接/断开操作
- 改进图层切换逻辑：根据当前视图模式显示正确内容
- 改进用户图层创建：只从根图层提取像素，不包括其他用户图层

#### 技术改进
- 统一用户图层背景色为灰色，表示未选中区域
- 完善历史管理器对用户图层的支持
- 优化参数加载逻辑，确保结构一致性
- 添加图层边界检查

---

## [v1.5.5.0] - 2026-03-28

### 修复
- 修复工具快捷键在用户图层上仍可激活的问题，现在快捷键会检查工具可用性
- 修复画笔痕迹在用户图层区域消失的问题，调整图层合成顺序
- 修复保存功能未合并用户图层的问题，保存时会正确合并所有可见图层

---

## [v1.5.4.0] - 2026-03-28

### 新增
- 合并图层时保留底层图层的二值化参数和原图区域，支持对合并后的图层重新调整二值化参数

### 修复
- 修复 Sauvola、Wolf、Nick 算法中标准差计算的浮点数精度警告
- 修复使用画笔后根图层不显示合成效果的问题
- 修复在用户图层上使用画笔时图层消失的问题

### 优化
- 改进画笔工具的临时层机制，确保在任何图层上绘制时都能看到完整的合成效果

---

## [v1.5.3.1] - 2026-03-27

### 界面更新
- 二值化面板添加"当前图层"显示区域

### 数据结构扩展
- UserLayer 类添加 `binarization_params` 字段保存二值化参数
- UserLayer 类添加 `original_region` 字段保存原图对应区域
- BinarizationPanel 添加 `get_all_params()` 和 `load_params()` 方法

---

## [v1.5.3.0] - 2026-03-27

### 新增功能
- **选择工具快捷操作**：
  - 在右侧面板快捷操作区添加"填充选区"按钮
  - 使用形态学闭运算和洪水填充算法填充选区内部空洞
  - 支持 OpenCV 和 SciPy 两种实现方式
  - 一键将圆圈选区变为实心选区

### 界面优化
- 图层面板高度调整

### 修复
- 图层切换工具状态修复
- 修复反选功能

---

## [v1.5.2.0] - 2026-03-27

### 性能优化：大图选择工具流畅度提升
- **异步轮廓更新**：
  - 轮廓计算移至后台线程，主线程不再阻塞
  
- **智能降采样策略**：
  - 根据图片尺寸和缩放级别动态调整轮廓质量
  - 小图片 (<4000px)：完整质量，无降采样
  - 中等图片 (4000-8000px)：根据缩放 1-3 倍降采样
  - 大图片 (>8000px)：根据缩放 2-5 倍降采样
  
- **动态节流更新**：
  - 小图片 (<3000px)：16ms 更新（60fps）
  - 中小图片 (3000-5000px)：40ms 更新
  - 中等图片 (5000-8000px)：80ms 更新
  - 大图片 (>8000px)：100ms 更新
  
- **局部重绘优化**：
  - 只重绘变化区域，而非整个画布
  - 大幅减少重绘开销

### 用户体验改进
- 小图片选择工具响应接近实时
- 大图片拖动更加流畅

---

## [v1.5.1.0] - 2026-03-27

### 选择工具改进
- **移除目标颜色限制**：
  - 选择工具不再限制颜色，圆形范围内所有像素都被选中
  - 改为使用虚线边框显示选择区域

### 填充功能改进
  - "智能填充"改为两个独立按钮："黑色"和"白色"

### 图层系统优化
- **保存选区功能**：
  - 保存选区时，保存当前合成后的完整图像
  - 切换到图层时，只显示选中区域
  - 未选中区域显示为灰色

---

## [v1.5.0.0] - 2026-03-27

### 重大更新：图层系统
- **新增图层面板**：支持多图层管理
  - 根图层：动态响应二值化参数调整
  - 用户图层：保存固化的像素区域，不受参数影响
  
- **核心功能**：
  - 保存选区为图层：将选中区域保存为独立图层
  - 图层切换：在根图层和用户图层之间自由切换
  - 图层合成：根图层 + 编辑层 + 用户图层自动合成显示
  - 删除图层：支持删除用户图层，根图层受保护
  - 合并图层：支持合并多个图层，使用最小编号命名

### 界面改进
- 右侧新增图层面板

### 测试框架
- 新增图层系统测试套件

---

## [v1.4.13.2] - 2026-03-27

### 问题修复
- **修复英文界面显示问题**：
  - 将视图模式 "Preprocessed" 缩短为 "Processed"

## [v1.4.13.1] - 2026-03-27

### 界面优化
- **关于对话框优化**：
  - 在关于对话框右侧添加 100x100 程序图标显示
  - 深色主题下自动使用深色版本图标（APP_ICON_BYTES_DARK）
  - 浅色主题下使用浅色版本图标（APP_ICON_BYTES）

- **程序图标主题适配**：
  - 程序窗口图标根据主题自动切换（深色/浅色）

---

## [v1.4.13.0] - 2026-03-27

### 新增功能
- **深色主题完整实现**：
  - 添加完整的深色主题样式文件 `themes/dark_theme.qss`
  - 实现主题切换功能（浅色/深色/跟随系统）
  - 支持 Windows 11 标题栏深色模式自动切换
  - 主题切换后立即生效，无需重启程序

- **跟随系统主题功能**：
  - 自动检测 Windows 系统深色/浅色模式设置
  - 程序主题和标题栏颜色自动跟随系统设置

---

## [v1.4.12.1] - 2026-03-26

### 问题修复
- **修复抓取工具状态问题**：
  - 修复未加载图片时抓取工具仍可点击的问题
  - 现在所有工具在未加载图片时都会被正确禁用

---

## [v1.4.12.0] - 2026-03-26

### 功能完善
- **文件保存设置功能实现**：
  - 实现默认保存格式配置（跟随原文件/PNG/JPG/BMP/WebP）
  - 实现文件名格式配置（时间戳/副本/自定义前后缀）
  - 添加 WebP 格式支持
  - 保存文件时自动应用用户配置

- **图像尺寸限制功能实现**：
  - 在加载图像前检查尺寸，避免加载过大图像导致内存溢出
  - 超出限制时显示友好提示，告知用户图像尺寸和限制值

### 问题修复
- **修复重置按钮动画问题**：
  - 在动画播放期间禁用按钮，防止重复触发

---

## [v1.4.11.0] - 2026-03-26

### 代码质量改进
- **集成 Ruff 代码检查工具**：
  - 添加 Ruff linter 和 formatter 配置
  - 修复多个代码质量问题
  - 修复 `binarization_panel.py` 中未定义变量的严重错误
  - 优化异常处理，添加异常链追踪
  - 简化条件语句，提高代码可读性
  - 统一代码格式，清理空白字符
  - 更新 Ruff 配置到最新结构

- **集成 Pylint 分析工具**：
  - 添加 Pylint 配置文件 `.pylintrc`
  - 支持重复代码检测（duplicate-code）

---

## [v1.4.10.0] - 2026-03-26

### 性能优化
- **预处理视图性能优化**：
  - 添加预处理异步处理，滑块调整不再卡顿
  - 使用防抖机制，避免频繁触发计算
  - 预处理和二值化共用防抖延迟配置（可在设置中调整）
  - 处理时显示"处理中"提示，体验与二值化视图一致

### 界面优化
- 更新设置界面的防抖延迟说明，明确适用于预处理和二值化
- 优化参数调整的响应体验，大幅提升流畅度

---

## [v1.4.9.0] - 2026-03-25

### 新增功能
- **二值化面板分页设计**：
  - 添加标签页组件，将设置项分为两个标签页
  - 预处理标签页：包含预处理参数和 RGB 通道调整（默认显示）
  - 二值化标签页：包含边缘检测、二值化方法和降噪功能
  - 标签页切换使用淡入淡出动画效果
  - 标签页内容区域圆角设计，选中标签与内容区域无缝连接
  - 界面组织更加清晰，功能分区明确

### 界面优化
- 优化左侧二值化面板布局，宽度调整为 300px
- 优化右侧属性面板布局，固定宽度为 250px
- 统一下拉框宽度为 130px，提升视觉一致性
- 简化标签文字，减少冗余
- 移除下拉框与标签之间的多余间距
- 标签页样式与整体主题风格保持一致
- 修复边缘强度滑块宽度异常问题，为容器添加正确的尺寸策略
- 统一所有滑块容器的布局行为

### 功能优化
- **边缘检测模式优化**：
  - 边缘检测模式设置为"关闭"时，自动隐藏边缘强度和阈值滑块
  - Canny 模式显示强度和阈值两个滑块
  - 其他边缘检测模式只显示强度滑块
  - 界面更加简洁，减少无效控件显示

### 新增
- 添加画布背景色设置功能

---

## [v1.4.8.1] - 2026-03-25

### 修复
- 修复英文界面下左侧面板挤压画布区域的布局问题，优化面板宽度和拉伸因子

---

## [v1.4.8.0] - 2026-03-25

### 优化
- 优化选择工具填充性能，使用画笔批量填充逻辑
- 优化选择工具拖动性能，缓存像素数据避免重复合成，解决第二次及后续使用卡顿问题
- 为未来的对象图层系统预留兼容性

### 修复
- 修复加载图片时未显示"处理中"提示的问题，改用异步加载

---

## [v1.4.7.0] - 2026-03-25

### 新增功能
- **抓取工具（Pan Tool）**：
  - 添加独立的抓取工具，用于拖动画布进行平移
  - 快捷键：H

---

## [v1.4.6.3] - 2026-03-25

### 界面优化
- 为选项卡（Tab）切换添加淡入淡出动画

---

## [v1.4.6.2] - 2026-03-25

### 界面优化
- 将复选框改为滑块开关，视觉效果更现代

---

## [v1.4.6.1] - 2026-03-25

### 界面优化
- 缩小单选按钮尺寸（16px → 12px）

---

## [v1.4.6.0] - 2026-03-25

### 新增功能
- **动画全局开关**：
  - 在设置对话框的"界面"标签页添加"启用 UI 动画"复选框
  - 设置立即生效，无需重启应用

- **设置界面优化**：
  - 将"通用"标签页的语言设置移到"界面"标签页
  - 删除"通用"标签页，简化设置结构

---

## [v1.4.5.0] - 2026-03-25

### 新增功能
- **重置按钮动画效果**：
  - 点击重置按钮时播放 360 度旋转动画

- **视图模式切换器滑动动画**：
  - 点击视图模式按钮时，选中指示器平滑滑动到目标位置

---

## [v1.4.4.0] - 2026-03-25

### 新增功能
- **参数重置按钮**：
  - 为二值化面板的所有滑块添加重置按钮
  - 点击重置按钮可快速恢复参数默认值

### 改进
- **UI 布局优化**：
  - 修复阈值滑块长度不一致的问题
  - 统一所有滑块的布局边距设置
  - 优化二值化方法下拉框的布局对齐

---

## [v1.4.3.2] - 2026-03-24

### 改进
- **缩放范围扩展**：
  - 缩放最小值从 10% 调整为 1%

---

## [v1.4.3.1] - 2026-03-24

### 改进
- **用体验优化**：
  - 加载图片时显示"处理中"提示
  - 优化标签文本处理，正确移除占位符

---

## [v1.4.3.0] - 2026-03-24

### 新增功能
- **多语言支持**：
  - 创建翻译管理器 (`TranslationManager`) 用于管理多语言翻译
  - 支持中文（zh_CN）和英文（en_US）两种语言
  - 使用 JSON 文件存储翻译字符串
  - 翻译文件位置：`locales/zh_CN.json`, `locales/en_US.json`
  - 支持嵌套键访问（如 `menu.file.open`）
  - 支持占位符替换（如 `"已加载: {filename}"`）
  - 在设置对话框中启用语言切换功能
  - 语言更改后提示用户重启应用生效
  - **所有 UI 组件已完整集成翻译系统**：
    - ✅ 主窗口 (MainWindow) - 完全翻译
    - ✅ 设置对话框 (SettingsDialog) - 完全翻译
    - ✅ 属性面板 (PropertiesPanel) - 完全翻译
    - ✅ 二值化面板 (BinarizationPanel) - 完全翻译
    - ✅ 所有状态消息和对话框 - 完全翻译
  - 翻译文件包含 200+ 条翻译键，覆盖所有用户可见文本

### 技术实现
- 创建翻译管理器 (`src/utils/translation_manager.py`)
- 创建完整的中英文翻译文件 (`locales/zh_CN.json`, `locales/en_US.json`)
- 所有主要 UI 组件集成翻译系统
- 配置管理器支持语言设置的保存和加载
- 翻译架构支持未来扩展更多语言

### 待完成
- 热重载语言切换

---

## [v1.4.2.0] - 2026-03-24

### 新增功能
- **工具栏增强**：
  - 添加"设置"按钮，打开设置对话框
  - 添加"关于"按钮，显示应用信息

- **完整的配置管理系统**：
  - 创建配置管理器 (`ConfigManager`) 用于统一管理应用配置
  - 配置文件位置：`data/config.json`（项目根目录）
  - 支持配置加载、保存、重置为默认值
  - UI 值与配置值自动映射转换
  - 最近文件列表管理
  - 完整的单元测试覆盖

### 用户体验改进
- **配置立即生效**：
  - 画笔大小、选择范围立即更新（工具和 UI 同步）
  - 撤销历史限制立即应用
  - Tile 缓存大小动态调整
  - 二值化防抖延迟实时更新
  - 属性面板 UI 自动同步显示新值

### 技术实现
- 创建独立的 `SettingsDialog` 类 (`src/views/settings_dialog.py`)
- 创建配置管理器 (`src/utils/config_manager.py`)
- 配置文件模板 (`data/config.template.json`)
- 完整的单元测试 (`tests/test_config_manager.py`)，测试隔离不影响实际配置
- 属性面板 UI 自动同步更新

---

## [v1.4.1.0] - 2026-03-24

### 构建系统改进
- **Nuitka 打包脚本**：
  - 新增完整的 `build.py` 打包脚本
  - 自动从 `src.__version__` 读取版本信息
  - 支持 Cython 模块自动编译和包含
  - 包含 themes 目录和图标文件（`icon.ico`）
  - 优化编译参数，排除不需要的模块减小体积
  - 自动清理旧的编译文件
  - 生成版本锁定文件（`.version`）用于版本管理
  - 输出文件夹命名为 `BinarizationToolv{version}`
  - 主可执行文件自动重命名为 `BinarizationTool.exe`
  - Windows 文件属性显示完整版本信息和版权信息

### Bug 修复
- **OpenCV 参数验证修复**（继承自 v1.4.0.1）：
  - 修复自适应阈值（Adaptive Threshold）窗口大小验证问题
  - 修复 Sauvola、Wolf、Nick、Bernsen 算法窗口大小验证问题
  - 新增 `_validate_window_size()` 辅助方法统一处理窗口大小验证
  - 确保所有窗口大小参数都是奇数且 >= 3，符合 OpenCV 要求

---

## [v1.4.0.1] - 2026-03-24

### Bug 修复
- **OpenCV 参数验证修复**：
  - 修复自适应阈值（Adaptive Threshold）窗口大小验证问题
  - 修复 Sauvola、Wolf、Nick、Bernsen 算法窗口大小验证问题
  - 新增 `_validate_window_size()` 辅助方法统一处理窗口大小验证
  - 确保所有窗口大小参数都是奇数且 >= 3，符合 OpenCV 要求
  - 解决小图像或特殊尺寸图像处理时的报错

### 技术改进
- 统一窗口大小验证逻辑，提高代码可维护性
- 添加详细的参数验证文档说明

---

## [v1.4.0.0] - 2026-03-24

### 重大性能提升 🚀
- **Cython 加速引擎**：
  - Floyd-Steinberg 抖动：原生编译，处理速度极快
  - Atkinson 抖动：原生编译，Mac 风格艺术效果
  - Ordered 抖动：原生编译，Bayer 矩阵网点效果
  - 大图像抖动处理实时化，支持实时预览

### 技术架构升级
- **Cython 原生编译**：
  - 创建 `src/cython_core` 模块，使用 C 级优化
  - 三种抖动算法全部编译为原生 DLL/SO 文件
  - 使用内存视图（memoryview）和 nogil 优化
  - 释放 GIL 提升并发性能
  
- **智能 DLL 加载**：
  - 支持开发环境和打包环境的 DLL 路径自动检测
  - PyInstaller 和 Nuitka 打包完全兼容
  - 根目录 DLL 搜索支持（打包后场景）

- **编译工具链**：
  - 新增 `setup.py` 编译配置
  - 新增 `compile_cython.py` 便捷编译脚本
  - 新增 `requirements-dev.txt` 开发依赖

### 质量保证
- 所有 47 个测试用例通过
- 零 API 变化，完全向后兼容

### 用户体验
- 实时预览抖动效果
- 大图像处理流畅不卡顿
- 后台线程处理，UI 响应迅速
- 无感知的性能提升（无状态消息干扰）

### 兼容性
- 完全兼容 Nuitka 打包
- 完全兼容 PyInstaller 打包
- 零 API 变更，现有代码无需修改

---

## [v1.3.5.0] - 2026-03-23

### 新增功能
- **抖动算法（Dithering）**：
  - 添加 3 种抖动算法作为新的二值化方法
  - Floyd-Steinberg 抖动：经典误差扩散算法，保留灰度细节
  - Ordered 抖动：使用 Bayer 矩阵产生网点效果，适合打印
  - Atkinson 抖动：Mac 风格的轻量级误差扩散，适合艺术效果
  - 抖动算法特别适合照片和复杂图像的二值化

### 用户体验改进
- **智能参数显示**：
  - Floyd-Steinberg 和 Atkinson：显示"抖动强度"滑块（0-100）
  - Ordered 抖动：显示"矩阵大小"滑块（2/4/8/16）
  - 参数根据选择的抖动算法自动显示/隐藏
  - 在二值化方法下拉框中添加分隔线，区分传统方法和抖动算法

### 技术实现
- **BinarizationEngine 扩展**：
  - 新增 `apply_floyd_steinberg()` 方法：经典 7/16 权重误差扩散
  - 新增 `apply_ordered_dithering()` 方法：递归生成 Bayer 矩阵
  - 新增 `apply_atkinson()` 方法：1/8 权重误差扩散，6 个方向
  - `apply_threshold()` 方法支持方法 7-9（抖动算法）

- **BinarizationPanel 增强**：
  - 二值化方法下拉框扩展到 10 个选项（7 个传统 + 3 个抖动）
  - 添加抖动参数容器，包含强度和矩阵大小滑块
  - `get_method_params()` 支持获取抖动参数
  - `_update_threshold_enabled()` 智能控制抖动参数显示

### 算法说明
- **Floyd-Steinberg**：
  - 误差扩散到右侧和下方 4 个像素
  - 权重：右 7/16，左下 3/16，下 5/16，右下 1/16
  - 效果：细节丰富，适合大多数图像

- **Ordered Dithering**：
  - 使用 Bayer 矩阵作为阈值图
  - 产生规则的网点图案
  - 效果：适合打印，有序的点阵

- **Atkinson**：
  - 误差扩散到右侧、右右侧、下方 3 个方向和下下方
  - 权重：每个方向 1/8
  - 效果：更轻的扩散，保留高光，艺术风格

### 使用场景
- **照片转黑白**：使用 Floyd-Steinberg 或 Atkinson
- **打印优化**：使用 Ordered 抖动
- **艺术创作**：使用 Atkinson 抖动
- **文档扫描**：使用传统二值化方法（Otsu、自适应等）

### 性能说明
- 抖动算法是逐像素处理，比传统二值化稍慢
- 已集成到异步处理流程，不会阻塞 UI
- 大图像处理时会显示"处理中..."提示

---

## [v1.3.4.0] - 2026-03-23

### 新增功能
- **所有二值化方法的可调参数**：
  - 为全部 7 种二值化方法添加用户可调参数
  - 参数根据选择的方法动态显示/隐藏
  - 固定阈值：阈值（0-255）
  - 自适应阈值：阈值、块大小（3-51 奇数）
  - Otsu 自动阈值：无参数
  - Sauvola 阈值：窗口大小（3-51）、k 参数（0.0-1.0）、R 参数（0-255）
  - Wolf 阈值：窗口大小（3-51）、k 参数（0.0-1.0）
  - Nick 阈值：窗口大小（3-51）、k 参数（-1.0-0.0）
  - Bernsen 阈值：窗口大小（3-51）、对比度阈值（0-255）

### 用户体验改进
- **智能参数显示**：
  - 切换二值化方法时，自动显示对应的参数滑块
  - 不需要的参数自动隐藏，界面简洁
  - 参数直接显示在二值化方法选择器下方，无需弹出窗口
  - 所有参数实时预览效果

- **默认值优化**：
  - 窗口大小参数默认值基于图像尺寸自动计算（image_size // 8）
  - 用户可以从默认值开始调整
  - 所有参数都有合理的默认值，开箱即用

### 技术实现
- **BinarizationEngine 扩展**：
  - `apply_threshold()` 方法支持 `**kwargs` 接收方法特定参数
  - 所有 7 种方法都支持自定义参数
  - 自动计算窗口大小（未指定时）
  - 参数验证和范围限制

- **BinarizationPanel 增强**：
  - 为每种方法创建独立的参数容器
  - `get_method_params()` 方法获取当前方法的参数
  - `_update_threshold_enabled()` 控制参数容器的显示/隐藏
  - 所有参数滑块连接到 `_on_parameters_changed()` 信号

- **BinarizationWorker 更新**：
  - 接收 `method_params` 参数
  - 在 `run()` 方法中传递参数到 `apply_threshold()`

- **MainWindow 集成**：
  - `_on_parameters_changed()` 获取方法参数
  - `_start_binarization()` 传递参数到工作线程
  - `_load_file_from_path()` 初始加载时使用方法参数

### 参数说明
- **窗口大小/块大小**：控制局部区域的大小，影响算法的局部适应性
- **k 参数**：控制阈值的敏感度，不同方法含义略有不同
- **R 参数**（Sauvola）：标准差的动态范围，通常为 128
- **对比度阈值**（Bernsen）：判断区域是否有足够对比度的阈值

### 用户收益
- 可以精细调整每种二值化方法的效果
- 不同类型的图像可以找到最佳参数组合
- 实时预览让参数调整更加直观
- 界面简洁，只显示当前方法需要的参数

---

## [v1.3.3.0] - 2026-03-23

### 新增功能
- **边缘检测增强**：
  - 在二值化面板添加"边缘检测"分组（位于 RGB 通道和二值化之间）
  - 四种边缘检测模式：
    - 关闭：不进行边缘检测
    - Canny 边缘：提取清晰轮廓线，适合人物剪影
    - 边缘增强：保留原图细节同时强化边缘，适合风景
    - 轮廓保留：形态学梯度，保持物体边界
  - 边缘强度控制（0-100）：
    - Canny 模式：控制低阈值
    - 增强模式：控制叠加权重
    - 轮廓模式：控制形态学核大小
  - 边缘阈值控制（0-255）：仅 Canny 模式显示，控制高阈值
  - 实时预览边缘检测效果

### 用户体验改进
- **处理中提示**：
  - 在画布顶部居中显示小巧的"处理中..."提示
  - 白色半透明背景，深灰色文字
  - 圆角边框，与主题风格匹配
  - 处理完成后自动消失
  - 不遮挡主要工作区域

### 技术实现
- **ImageEnhancer 扩展**：
  - 添加 `apply_edge_detection()` 方法
  - 支持 Canny、Sobel 梯度、形态学梯度三种算法
  - 智能参数映射和阈值计算

- **Canvas 处理状态**：
  - 添加 `is_processing` 状态标志
  - 添加 `set_processing()` 方法控制状态
  - 添加 `_render_processing_indicator()` 方法渲染提示
  - 主窗口在启动/完成二值化时更新状态

- **UI 智能控制**：
  - 边缘阈值滑块仅在 Canny 模式下显示
  - 其他模式下完全隐藏边缘阈值控件
  - 参数变化时自动更新控件显示状态

### 用户体验改进
- 为人物和风景图像提供更好的二值化效果
- 边缘信息保留更加清晰
- 轮廓线提取更加准确
- 参数调整实时预览，便于找到最佳效果
- 处理状态清晰可见，用户体验更好

## [v1.3.2.0] - 2026-03-23

### 问题修复
- **修复裁剪工具快捷键 C 无法使用的问题**：
  - 添加 `self.addAction(self.crop_action)` 启用快捷键
  - 现在按 C 键可以正常激活裁剪工具

### 用户体验改进
- **选择工具矩形框选光标优化**：
  - 矩形框选模式下显示自定义十字光标（隐藏系统光标）
  - 十字颜色和框线颜色都根据选择模式变化：
    - 绿色十字 + 绿色框线 = 添加到选区
    - 红色十字 + 红色框线 = 从选区删除
  - 避免系统光标和自定义光标叠加显示

- **裁剪工具光标优化**：
  - 裁剪工具使用自定义红色十字光标
  - 框线改为红色虚线
  - 移除框线外的黑色边框

### 技术实现
- 在 `CursorRenderer` 中添加 `render_crosshair_cursor()` 方法
- 在 `SelectionTool` 中添加 `render_rect_cursor()` 方法
- 在 `CropTool` 中添加 `render_cursor()` 方法
- 矩形框选模式和裁剪模式下都设置 `Qt.BlankCursor` 隐藏系统光标
- 画布 `paintEvent` 中渲染自定义十字光标
- 修复 `main_window.py` 中切换模式时的光标设置冲突
- 移除 `RectSelector` 中的黑色边框

## [v1.3.1.0] - 2026-03-23

### 代码重构
- **涂抹类封装优化**：
  - 创建 `StrokeInterpolator` 工具类统一管理插值逻辑
  - 重构 `BrushTool` 使用 `StrokeInterpolator`
  - 重构 `SelectionTool` 使用 `StrokeInterpolator`
  - 消除约 40 行重复的插值代码
  - 提供三个静态方法：
    - `calculate_distance()` - 计算两点距离
    - `interpolate_points()` - 插值生成中间点
    - `calculate_last_interpolated_position()` - 计算最后插值点位置

### 技术改进
- 统一涂抹操作的插值算法
- 提高代码可维护性和可测试性
- 简化工具类代码，降低复杂度
- 所有测试通过，功能完全保持

## [v1.3.0.0] - 2026-03-22

### 新增功能
- **矩形框选模式**：
  - 为选择工具添加矩形框选功能
  - 复用裁剪工具的矩形选择逻辑
  - 在属性面板的"基础设置"中添加"方式"选项（涂抹/框选）
  - 使用单选按钮组切换选择方式
  - 矩形框选时显示绿色（添加）或红色（减去）边框
  - 支持拖动创建矩形选区
  - 只选择矩形区域内匹配目标颜色的像素
  - 与现有的拖动选择模式无缝切换

### 用户体验改进
- 矩形框选模式启用时光标变为十字
- 涂抹模式下显示圆圈光标
- 状态栏显示当前模式状态
- 矩形框选边框颜色根据选择模式动态变化
- 选择方式集成在基础设置中，界面更统一

### 技术实现
- SelectionTool 添加 `rect_select_mode` 属性
- 添加 `start_rect_select()`, `continue_rect_select()`, `end_rect_select()` 方法
- 添加 `render_rect_select_overlay()` 方法渲染矩形框选覆盖层
- Canvas 鼠标事件处理支持矩形框选模式
- 属性面板添加单选按钮组（涂抹/框选）
- MainWindow 添加 `_on_selection_method_changed_panel()` 方法处理方式切换
- 矩形框选只选择匹配目标颜色的像素，与手动涂抹行为一致

### 代码重构
- **模块复用优化**：
  - 创建 `RectSelector` 基类统一管理矩形选择逻辑
  - 创建 `CursorRenderer` 工具类统一管理光标渲染
  - 重构 `SelectionTool` 使用 `RectSelector` 和 `CursorRenderer`
  - 重构 `CropTool` 使用 `RectSelector`
  - 重构 `BrushTool` 使用 `CursorRenderer`
  - 消除约 70% 的矩形选择重复代码
  - 消除约 60% 的光标渲染重复代码
  - 提高代码可维护性和一致性
  - 所有测试通过，功能完全保持

### 新增功能
- **矩形框选模式**：
  - 为选择工具添加矩形框选功能
  - 复用裁剪工具的矩形选择逻辑
  - 在属性面板的"基础设置"中添加"方式"选项（涂抹/框选）
  - 使用单选按钮组切换选择方式
  - 矩形框选时显示绿色（添加）或红色（减去）边框
  - 支持拖动创建矩形选区
  - 只选择矩形区域内匹配目标颜色的像素
  - 与现有的拖动选择模式无缝切换

### 用户体验改进
- 矩形框选模式启用时光标变为十字
- 涂抹模式下显示圆圈光标
- 状态栏显示当前模式状态
- 矩形框选边框颜色根据选择模式动态变化
- 选择方式集成在基础设置中，界面更统一

### 技术实现
- SelectionTool 添加 `rect_select_mode` 属性
- 添加 `start_rect_select()`, `continue_rect_select()`, `end_rect_select()` 方法
- 添加 `render_rect_select_overlay()` 方法渲染矩形框选覆盖层
- Canvas 鼠标事件处理支持矩形框选模式
- 属性面板添加单选按钮组（涂抹/框选）
- MainWindow 添加 `_on_selection_method_changed_panel()` 方法处理方式切换
- 矩形框选只选择匹配目标颜色的像素，与手动涂抹行为一致

---

## [v1.2.5.0] - 2026-03-22

### 性能优化
- **选择工具性能大幅提升**：
  - 优化 TileCache 渲染逻辑，跳过没有选区的瓦片
  - 使用更快的灰度转 RGB 方法（直接赋值替代 np.stack）
  - 避免每次鼠标移动都复制整个图像数据
  - 添加节流机制，批量处理瓦片失效操作
  - 第二次及以后使用选择工具不再卡顿

### 技术改进
- **Canvas 优化**：
  - 选择工具移动时直接更新 tile_cache 的选区引用，避免复制
  - 添加选择更新节流定时器（16ms = 60fps）
  - 累积脏区域，批量失效瓦片
  - 立即触发重绘保持视觉流畅，延迟失效瓦片减少计算

- **TileCache 优化**：
  - 检查瓦片是否包含选区（selection_tile.any()）
  - 只对包含选区的瓦片进行 RGB 转换
  - 空选区瓦片保持灰度图渲染（零开销）
  - 使用 np.empty + 直接赋值替代 np.stack（更快）

- **SelectionTool 优化**：
  - 降低 spacing 参数从 0.25 到 0.1，提高插值密度
  - 移除距离阈值检查，每次鼠标移动都处理

### 用户体验改进
- 选择工具在任何时候都保持流畅
- 大选区操作不再影响性能
- 实时反馈更加顺畅

### 已知限制
- 快速转圈时选区边缘可能呈现多边形

---

## [v1.2.4.0] - 2026-03-22

### 新增功能
- **WebP 格式支持**：
  - 支持打开和保存 WebP 格式图片
  - 完整支持 WebP 的二值化处理

### 性能优化
- **提升 tile 缓存容量**：
  - 将 TileCache 的 max_tiles 从 100 提升到 1000
  - 改善大缩放级别下的画笔流畅度
  - 减少大图片拖动和缩放时的 tile 重新生成
  - 提供更流畅的大图片浏览体验
- **优化大图拖动性能**：
  - 拖动节流，限制重绘频率为 60fps（每 16ms）
  - 减少不必要的重绘调用，改善大型图片拖动流畅度

### UI 改进
- **修复文件名显示问题**：
  - 文件名最大显示 23 个字符
  - 完整文件名显示在工具提示中
- **增加界面宽度**：
  - 属性面板宽度从 250px 增加到 270px
  - 程序总宽度从 1450px 增加到 1550px

## [v1.2.3.0] - 2026-03-22

### 测试改进
- **新增测试覆盖**：
  - 为分块缓存（TileCache）添加完整测试套件
    - 13 个单元测试：初始化、图片设置、缓存管理、LRU 淘汰、区域失效等
    - 7 个 Hypothesis 属性测试：缓存大小限制、尺寸更新、边界检查、缓存一致性等
    - 测试覆盖率达到 92%
  - 为视图模式切换器（ViewModeSwitcher）添加完整测试套件
    - 11 个单元测试：初始化、模式设置、按钮点击、信号发射、按钮组互斥等
    - 7 个 Hypothesis 属性测试：模式更新、按钮状态、序列一致性、信号计数等
    - 测试覆盖率达到 100%

- **测试质量提升**：
  - Hypothesis 成功发现并验证了浮点数精度边界情况
  - 测试正确反映了代码的设计意图（如 set_scale 的 0.001 容差）
  - 所有 47 个测试全部通过 ✓
  - 整体测试覆盖率从 9 个测试提升到 47 个测试

---

## [v1.2.2.0] - 2026-03-22

### 新增功能
- **视图模式切换器**：
  - 在左侧二值化面板顶部添加视图模式切换器
  - 三种视图模式：
    - 原图：显示原始彩色图像
    - 预处理：显示预处理后的灰度图像
    - 二值化：显示二值化后的黑白图像（默认）
  - iOS 风格的分段控件设计
  - 选中状态使用浅蓝色半透明背景，易于区分
  - 实时切换，无需重新加载图片

- **视图模式切换快捷键**：
  - `Ctrl+1`：切换到原图模式
  - `Ctrl+2`：切换到预处理模式
  - `Ctrl+3`：切换到二值化模式
  - `Tab`：循环切换视图模式（原图 → 预处理 → 二值化 → 原图）
  - 按钮 tooltip 显示对应的快捷键提示
  - 快速切换视图，提升工作效率

- **智能视图更新**：
  - 原图模式：不受参数调整影响，始终显示原始图像
  - 预处理模式：实时显示预处理效果（曝光、对比度、锐化等）
  - 二值化模式：实时显示二值化结果
  - 参数调整时自动根据当前模式更新显示

- **工具状态管理**：
  - 画笔和选择工具仅在二值化模式下可用
  - 原图和预处理模式下自动禁用编辑工具
  - 裁剪工具在所有模式下可用
  - 裁剪后自动清除预处理缓存

### 界面优化
- **面板布局改进**：
  - 视图模式切换器和二值化设置分成两个独立的白色背景区域
  - 每个区域有独立的圆角边框和内边距
  - 视觉层次更加清晰，功能分区明确

### 技术实现
- **ImageData 扩展**：
  - 添加 `view_mode` 属性（'original', 'preprocessed', 'binary'）
  - 添加 `preprocessed_pixels` 属性存储预处理结果
  - 新增 `set_view_mode()` 方法切换视图模式
  - 新增 `set_preprocessed_pixels()` 方法设置预处理结果
  - 新增 `invalidate_preprocessed_cache()` 方法清除预处理缓存
  - `get_current_pixels()` 方法根据视图模式返回对应图像

- **TileCache 彩色图像支持**：
  - 修改 `set_image()` 和 `update_image()` 支持 3D 数组（彩色图像）
  - 修改 `_generate_tile()` 自动检测并正确渲染彩色/灰度图像
  - 选区覆盖层在彩色图像上正确显示

- **Canvas 渲染优化**：
  - 移除所有不必要的灰度转换
  - 原图模式下保留彩色信息
  - 预处理和二值化模式下显示灰度图

- **MainWindow 集成**：
  - 监听视图模式切换信号
  - 根据模式更新工具状态（启用/禁用）
  - 参数调整时根据模式决定是否重新计算
  - 裁剪操作自动清除预处理缓存

### 用户体验提升
- 可以随时查看原图、预处理效果和二值化结果
- 方便对比不同处理阶段的效果
- 调试预处理参数更加直观
- 界面布局更加清晰，功能分区明确

### 性能优化
- 预处理结果缓存，避免重复计算
- 原图模式下不触发任何计算
- 参数调整时智能判断是否需要重新计算

---

## [v1.2.1.0] - 2026-03-21

### 新增功能
- **RGB 通道调整**：
  - 在左侧二值化面板的预处理组件下方添加"RGB 通道"分组
  - 提供三个独立的通道调整滑块：
    - 红色通道：-100 到 100
    - 绿色通道：-100 到 100
    - 蓝色通道：-100 到 100
  - 可以精细调整原图的 RGB 通道，优化彩色图像的二值化效果
  - 实时预览调整效果
  - 如果输入是灰度图，会自动转换为 RGB 进行处理

### 技术实现
- 在二值化引擎的预处理流程中集成 RGB 通道调整
- 调整范围映射：-100 到 100 → -255 到 +255 像素值
- 使用 `np.clip()` 确保像素值在有效范围内
- 支持灰度图自动转换为 RGB

### 用户体验提升
- 为彩色图像二值化提供更多控制选项
- 可以通过调整通道来增强或减弱特定颜色
- 与其他预处理参数无缝集成

---

## [v1.2.0.1] - 2026-03-21

### 界面优化
- **按钮样式优化**：
  - 将属性面板中的按钮颜色从鲜艳的蓝色改为柔和的灰白色系
  - 与整体主题风格保持一致
  - 默认状态：浅灰色背景，深灰色文字，带浅灰色边框
  - 悬停和按下状态：渐进式加深，提供清晰的视觉反馈

---

## [v1.2.0.0] - 2026-03-21

### 界面优化

#### 左侧二值化面板
- **分区标题**：
  - 为预处理、二值化、降噪三个功能区添加标题
  - 使用 QGroupBox 为每个区域添加边框
  - 标题居中显示在边框线上
  - 统一的样式：1px 灰色边框，4px 圆角
- **面板宽度调整**：
  - 宽度从 240px 增加到 290px
  - 提供更宽敞的操作空间

#### 右侧属性面板
- **整体样式优化**：
  - 参考左侧面板样式，统一界面风格
  - 添加白色圆角背景容器
  - 宽度设置为 250px
- **分页按钮美化**：
  - 未选中标签：浅灰色背景，灰色文字，带边框
  - 选中标签：白色背景，深色文字，加粗字体
  - 悬停效果：未选中标签悬停时背景变深
  - 顶部圆角设计，与内容区域融合
- **内容区域边框**：
  - 整个选项卡区域（包括标签和内容）添加统一边框
  - 选中标签与内容区域边框融合，形成完整组件
  - 内部 QGroupBox 与外框保持适当间距（8px 内边距）
- **滚动支持**：
  - 添加滚动区域，内容过多时可滚动查看

#### 主窗口
- **窗口宽度调整**：
  - 总宽度从 1400px 增加到 1450px
  - 适应左右面板宽度增加

### 新增资源

#### 主题系统
- **添加主题管理器** (`theme_manager.py`)：
  - 支持加载和应用 QSS 主题文件
- **浅色主题文件** (`themes/light_theme.qss`)：
  - 完整的 QSS 样式定义
  - 统一的颜色方案和组件样式
- **资源管理** (`resources.py`)：
  - 集中管理应用资源（图标、图片等）

### 技术改进
- **代码组织优化**：
  - 主题相关代码独立到 `theme_manager.py`
  - 资源文件集中管理
  - 样式定义从代码中分离到 QSS 文件
- **二值化引擎优化**：
  - 改进预处理流程
  - 优化参数处理逻辑

### 用户体验提升
- 界面更加美观统一

---

## [v1.1.4.0] - 2026-03-21

### 性能优化
- **异步二值化处理**：
  - 使用 QThread 在后台线程执行二值化操作
  - UI 不再卡顿，参数调整更流畅
  - 防抖机制（150ms）：快速调整参数时只处理最后一次
  - 自动取消过时的处理任务
  - 状态栏显示处理进度（"处理中..."/"处理完成"）

### 新增功能
- **命令行支持**：
  - 支持通过命令行传入图片路径直接打开
  - 用法：`python main.py image.png`

### 问题修复
- **属性面板尺寸显示**：
  - 修复裁剪后属性面板仍显示原始尺寸的问题
  - 现在始终显示当前画布中图片的实际尺寸
  - 撤销/重做时自动更新尺寸信息

### 技术实现
- **BinarizationWorker 类**：
  - 后台线程执行预处理和二值化
  - 支持取消正在运行的任务
  - 错误处理和信号通知
- **防抖定时器**：
  - QTimer 实现 150ms 延迟
  - 避免频繁触发耗时计算
  - 智能任务管理，新任务自动取消旧任务

### 用户体验改进
- 二值化参数调整时 UI 保持响应
- 可以在处理时继续操作其他功能
- 快速拖动滑块不会触发多次计算
- 属性面板信息始终准确
- 支持命令行快速打开图片

---

## [v1.1.3.0] - 2026-03-21

### 新增功能
- **重置按钮**：
  - 在工具栏添加重置按钮（位于后退/前进按钮之后）
  - 快捷键：`Ctrl+R`
  - 点击后弹窗确认，避免误操作
  - 重置到初始状态：清除所有画笔编辑和裁剪操作
  - 恢复到刚打开图片时的二值化结果
  - 重置后清除历史记录，无法撤销
  - 只有在有编辑内容时才启用（智能检测画笔编辑和裁剪）

### 用户体验改进
- 提供快速恢复初始状态的方法
- 弹窗确认避免误操作
- 按钮状态智能显示（无编辑时禁用）
- 重置功能通过重新加载原始文件实现，可靠性高

---

## [v1.1.2.0] - 2026-03-21

### 新增功能
- **右侧属性面板**：
  - 三栏布局：左侧二值化面板 + 中间画布 + 右侧属性面板
  - 分页设计：
    - 第一页（属性）：显示图片信息
      - 文件名
      - 图片尺寸（宽 x 高）
      - 文件大小
      - 实时缩放比例
    - 第二页（工具）：工具设置区域
      - 统一的"基础设置"标题
      - 画笔工具：大小、颜色
      - 选择工具：范围、模式、目标颜色、填充选区
      - 选择工具独立"快捷操作"组：取消选择、反选
      - 裁剪工具：显示"该工具无设置项"
      - 未选择工具：显示"请选择一个工具"
  - 未加载图片时显示空状态
  - 加载图片后自动更新信息
  - 切换工具时自动切换到"工具"页

### 重大改进
- **工具设置整合**：
  - 所有工具设置移动到右侧属性面板
  - 删除了旧的悬浮设置面板和右键菜单
  - 根据当前工具自动显示对应设置
  - 快捷键操作同步更新属性面板控件
  - 统一的设置界面，更易维护

### 问题修复
- **选择工具优化**：
  - 修复快速移动时出现断点的问题（添加插值逻辑）

### 用户体验改进
- 布局更加清晰，功能分区明确
- 实时显示图片属性和缩放比例
- 切换工具时自动显示对应设置页

---

## [v1.1.1.0] - 2026-03-21

### 新增功能
- **右键快捷菜单**：
  - 画笔工具和选择工具支持右键调出设置面板
  - 在画布任意位置右键即可快速调整工具参数
  - 选择工具右键菜单新增快捷操作：
    - 智能填充：根据目标颜色自动填充相反颜色
      - 选择黑色区域 → 填充白色
      - 选择白色区域 → 填充黑色
    - 取消选择：快速清除当前选区
    - 反选：快速反转选区
  - 无需移动鼠标到工具栏，提升操作效率

### 用户体验改进
- 右键菜单位置跟随鼠标，操作更便捷
- 设置面板与工具栏悬浮面板保持一致
- 快捷操作按钮集成在设置面板中
- 智能填充按钮根据目标颜色动态调整文本

---

## [v1.1.0.0] - 2026-03-21

### 重大更新

#### 新增：智能选择工具
- **红色覆盖层显示**：
  - 选区使用红色覆盖层显示，性能优异
  - 集成到分块渲染系统（TileCache）
  - 拖动选择时实时动态更新，流畅无卡顿

- **核心实现**：
  - 创建 `SelectionStroke` 类，复用画笔工具逻辑
  - 使用 NumPy 向量化操作，高性能处理
  - 增量光栅化，只处理新添加的点
  - 间距控制避免重复选择

- **目标颜色选择**：
  - 在设置面板中添加颜色选项（黑色/白色）
  - 只选择指定颜色的像素块
  - 快捷键 `Ctrl+Shift+X` 快速切换目标颜色
  - 光标中心圆点显示目标颜色：黑点=选择黑色，白点=选择白色

- **选择逻辑优化**：
  - 简化算法：直接选择指示器范围内的所有目标颜色像素
  - 只选择半径范围内的像素，不扩展到整个连续块
  - 边缘像素容差优化（0.71 像素），确保边缘块被正确选中

#### 用户界面改进
- **工具栏优化**：
  - 隐藏菜单栏，界面更简洁
  - 所有功能通过工具栏按钮和快捷键访问
  - 画笔和选择按钮改为扁平样式
  - 与裁剪工具样式保持一致

- **快捷键优化**：
  - 移除 `Ctrl+Q` 退出快捷键（避免误触）
  - 移除 `Ctrl+A` 全选功能（不常用）
  - 修复 `W` 快捷键无法激活选择工具的问题
  - 工具切换时正确更新所有按钮状态

- **光标指示器增强**：
  - 选择光标同时显示模式和目标颜色
  - 外圈颜色：绿色=添加模式，红色=删除模式
  - 中心圆点：黑色=选择黑色块，白色=选择白色块

### 问题修复
- **快捷键冲突修复**：
  - 修复了 `Ctrl+X` 快捷键在画笔和选择工具之间的冲突
  - 修复了 `Ctrl+↑/↓` 快捷键在画笔和选择工具之间的冲突
  - 现在快捷键会根据当前激活的工具智能分发
  - 画笔工具激活时：`Ctrl+X` 切换颜色，`Ctrl+↑/↓` 调整画笔大小
  - 选择工具激活时：`Ctrl+X` 切换模式，`Ctrl+↑/↓` 调整选择范围

- **选区尺寸检查**：
  - 修复了裁剪图片后选区尺寸不匹配导致的崩溃问题
  - 裁剪后自动清除选区
  - 选区操作时自动检查尺寸匹配，不匹配时重置选区

- **选择算法修复**：
  - 修复了无法选择浮游黑色方块的问题
  - 修复了边缘像素选择不完整的问题
  - 修复了选区操作（Ctrl+D、Ctrl+Shift+I）不显示的问题

### 用户体验改进
- **画笔颜色指示器**：
  - 画笔光标现在使用彩色指示器
  - 红色圆圈和十字 = 黑色画笔（绘制黑色像素）
  - 绿色圆圈和十字 = 白色画笔（绘制白色像素）
  - 一眼就能看出当前画笔颜色

### 代码架构
- **快捷键处理模块化**：
  - 创建独立的 `ShortcutHandler` 类管理所有工具快捷键
  - 从主窗口中提取快捷键处理逻辑
  - 代码更加模块化和易于维护

- **选择工具实现**：
  - 复用画笔工具的核心逻辑（BrushStroke 模式）
  - NumPy 向量化操作，性能优异

### 新增功能
- **智能选择工具**：
  - 圆圈光标指示选择范围（类似画笔工具）
  - 可调节选择范围大小（1-500像素）
  - **支持拖动选择**：按住左键拖动连续选择多个区域
  - 智能间距控制：避免重复选择相同区域
  - 两种选择模式：
    - 添加模式（绿色圆圈）：添加到选区
    - 删除模式（红色圆圈）：从选区减去
  - 两种目标颜色：
    - 黑色（黑色中心点）：只选择黑色像素
    - 白色（白色中心点）：只选择白色像素
  - 快捷键支持：
    - `W`：激活选择工具
    - `Ctrl+X`：切换添加/删除模式
    - `Ctrl+Shift+X`：切换目标颜色
    - `Ctrl+↑/↓`：调整选择范围（智能步长）
  - 悬浮设置面板：鼠标悬停显示快速设置
  - 选区以红色覆盖层显示（高性能）

- **选区管理系统**：
  - 取消选择（Ctrl+D）：清除当前选区
  - 反选（Ctrl+Shift+I）：反转选区
  - 按颜色选择：从菜单选择所有黑色或白色像素
  - 智能填充选区：根据目标颜色自动填充相反颜色
    - 选择黑色区域 → 填充白色
    - 选择白色区域 → 填充黑色
    - 填充后自动清除选区，立即看到效果
    - 填充内容保存到编辑图层，不受二值化参数影响
    - 支持撤销/重做
  - 选区与撤销/重做系统集成

### 技术实现
- **SelectionStroke 类**：
  - 类似 BrushStroke，复用画笔逻辑
  - 增量光栅化，只处理新添加的点
  - NumPy 向量化操作，性能优异
  - 返回脏区域，支持增量缓存更新

- **TileCache 集成**：
  - 选区蒙版集成到分块渲染系统
  - 选中像素显示为红色 RGB(255, 0, 0)
  - 支持 RGB 和灰度图自动切换
  - 脏区域追踪，只更新变化的块

- **ImageData 集成**：
  - 添加 `selection_mask` 属性
  - 选区在 `copy()` 时正确复制
  - 裁剪时自动清除选区

- **Canvas 渲染优化**：
  - 选区通过 TileCache 渲染，无需额外绘制
  - 拖动选择时使用增量更新
  - 脏区域失效机制，性能最优

- **主窗口集成**：
  - 选择工具按钮和快捷键
  - 选区操作菜单项（隐藏菜单栏，通过快捷键访问）
  - 工具状态同步
  - UI 状态根据选区自动更新
  - 使用 ShortcutHandler 统一管理快捷键

### 用户体验提升
- 选区功能让精确编辑成为可能
- 红色覆盖层清晰显示选区范围，性能优异
- 快捷键操作高效便捷，无冲突
- 选区与现有工具无缝集成
- 画笔颜色一目了然
- 界面简洁，隐藏菜单栏

### 性能特点
- 使用 NumPy 向量化批量像素处理
- 分块渲染系统集成，只更新变化区域
- 增量光栅化，避免重复计算
- 脏区域追踪，最小化缓存更新

---

## [v1.0.4.0] - 2026-03-20

### 新增功能
- **降噪功能增强**：
  - 独立的降噪方法选择器（6种方法）
  - 保留原有的"平滑"功能（简单高斯模糊）
  - 新增形态学降噪：
    - 开运算（去除小的孤立噪点）
    - 闭运算（填充小孔）
  - 降噪方法：高斯、中值滤波、双边滤波、NLMeans、形态学开/闭运算

- **画笔工具快捷键**：
  - `Ctrl+X`：切换黑白颜色
  - `Ctrl+↑`：增大画笔（智能步长）
  - `Ctrl+↓`：减小画笔（智能步长）
  - 智能步长：小画笔(<20)步长1，中画笔(20-50)步长5，大画笔(>50)步长10
  - 状态栏实时显示当前设置

### 用户体验改进
- 禁用工具栏右键菜单，防止误操作关闭工具栏
- 画笔快捷键操作更加便捷
- 降噪和平滑可以独立使用或组合使用

### 技术改进
- 使用 QAction 实现全局快捷键，更可靠
- 降噪方法支持 6 种算法
- 预处理流程：平滑 → 降噪 → 其他处理

---

## [v1.0.3.0] - 2026-03-20

### 性能优化
- **分块渲染系统（Tile-based Rendering）**：
  - 将图片分成 256x256 的块，按需生成和缓存
  - 只渲染视口内可见的块，大幅减少渲染开销
  - 平移视图时无需重新生成图片，直接使用缓存块
  - 缩放时只更新可见区域的块
  - LRU 缓存策略，限制内存使用（最多 100 个块）
  - 平移性能提升，缩放性能提升

### 新增功能
- **智能保存逻辑**：
  - 第一次保存（Ctrl+S）：弹出对话框，默认文件名为 `原文件名_时间戳.扩展名`
  - 第二次及之后保存：直接覆盖第一次保存的文件，无需重复选择
  - 另存为（Ctrl+Shift+S）：总是弹出对话框
  - 避免意外覆盖原文件，同时提供快速保存便利

- **工具栏改进**：
  - 所有按钮显示快捷键提示（打开、保存、后退、前进、画笔、裁剪）
  - 右侧显示"当前工具"状态指示器
  - 裁剪工具使用十字光标，画笔工具隐藏光标

### 技术实现
- **TileCache 类**：
  - 分块管理器，处理块的生成、缓存和淘汰
  - 支持多缩放级别的独立缓存
  - 脏区域追踪，只更新受影响的块
  - OrderedDict 实现 LRU 淘汰策略
  - `update_image()` 方法支持增量更新
  - C-contiguous 内存布局确保 QImage 兼容性
  
- **Canvas 渲染优化**：
  - 移除整图缓存，改用分块渲染
  - 视口裁剪，只处理可见区域
  - 懒加载，块在需要时才生成
  - 画笔绘制时实时更新脏区域
  - 精确的坐标计算，减少接缝线
  - 工具专属光标（画笔：隐藏，裁剪：十字）

- **保存逻辑优化**：
  - 使用 `saved_file_path` 跟踪已保存的文件
  - 打开新文件时自动重置保存状态
  - 判断是覆盖还是另存为

### 用户体验提升
- 像素级缩放时平移不再卡顿，丝滑流畅
- 大图片处理更加流畅
- 画笔绘制实时响应，无延迟
- 保存操作更加智能和便捷
- 工具栏信息更加清晰，快捷键一目了然
- 工具光标更加直观

### 已知限制
- 某些缩放级别下可能出现轻微的块接缝线（分块渲染的常见权衡）

---

## [v1.0.2.0] - 2026-03-20

### 新增功能
- **画笔设置悬浮面板**：
  - 鼠标悬停在画笔工具按钮上 0.5 秒显示设置面板
  - 可调节画笔大小（1-500 像素）
  - 可选择颜色（黑色/白色）
  - 快速点击按钮激活工具，悬停显示设置
  - 移出区域自动隐藏

- **智能画笔光标**：
  - 使用画笔工具时隐藏系统光标，只显示圆圈指示器
  - 圆圈小于 30 像素时在外侧显示红色十字准星
  - 十字准星不遮挡圆圈内部，便于精确定位
  - 红色、2 像素粗、8 像素长的十字线

### 性能优化
- **画笔绘制性能大幅提升**：
  - 实现增量光栅化算法，只处理新增的笔画点，避免重复绘制
  - NumPy 向量化批量像素处理，替代 Python 双重循环
  - 综合性能提升使大画笔和像素级缩放下的绘制更加流畅

### 技术改进
- **增量光栅化系统**：
  - `BrushStroke.rasterize()` 支持 `start_index` 参数
  - Canvas 记录已光栅化的点数 (`rasterized_point_count`)
  - 鼠标移动时只处理新增点
  - 避免重复计算和绘制

- **NumPy 向量化优化**：
  - 使用 `np.meshgrid` 创建坐标网格
  - 向量化距离计算和掩码生成
  - 批量设置像素值，替代逐像素循环
  - 同步更新 `temp_layer` 和 `temp_edit_mask`

- **画笔光标优化**：
  - 十字准星分为四段，显示在圆圈外侧
  - 圆圈和十字之间有 2 像素间隙
  - 使用 `Qt.BlankCursor` 隐藏系统光标

- **UI 交互改进**：
  - 画笔按钮使用 QPushButton 支持悬浮事件
  - 延迟显示设置面板避免干扰点击
  - 快捷键 B 正常工作

### 用户体验提升
- 画笔绘制更加流畅，无明显延迟
- 小画笔时十字准星帮助精确定位
- 大画笔时只显示圆圈，视野更清晰
- 设置面板悬浮显示，不占用界面空间

### 问题修复
- 修复了 NumPy 向量化后画笔痕迹消失的问题
- 确保 `temp_edit_mask` 与 `temp_layer` 同步更新
- 画笔笔画现在能正确保存到编辑图层

---

## [v1.0.1.0] - 2026-03-20

### 新增功能
- **拖放支持**：支持将图片文件直接拖放到预览区域加载
  - 支持的格式：PNG, JPG, JPEG, BMP
  - 拖放后自动应用二值化处理

### 重大改进
- **无需切换编辑模式**：
  - 移除了"进入编辑模式"按钮
  - 画笔和裁剪工具可以直接使用，无需切换模式
  - 简化了工作流程，提升用户体验

- **独立编辑图层系统**：
  - 画笔和裁剪操作在独立的编辑图层中进行
  - 使用编辑掩码（mask）存储用户编辑的像素差异
  - **画笔痕迹完全不受二值化参数影响**
  - 只有实际被画笔触碰的像素才会被标记为编辑过的
  - 修改二值化参数时，画笔编辑的内容保持不变
  - 未被编辑的区域会随着二值化参数实时更新
  - 支持在编辑后继续调整二值化参数

- **保存逻辑优化**：
  - 保存文件（Ctrl+S）现在默认为另存为操作
  - 自动生成文件名格式：`原文件名_时间戳.扩展名`
  - 例如：`image.png` → `image_20260320_143025.png`
  - 避免意外覆盖原始图片

### 技术改进
- **图层架构重构**：
  - 基础层（`pixels`）：存储二值化结果，可随时更新
  - 编辑掩码（`edit_mask`）：布尔数组，标记被编辑的像素
  - 编辑值（`edit_values`）：存储用户编辑的像素值
  - 临时层（`temp_layer`）：绘制时的实时预览
  - 临时编辑掩码（`temp_edit_mask`）：记录当前笔画中被触碰的像素
  - 合成显示：基础层 + 编辑掩码覆盖 = 最终显示

- **精确的编辑追踪**：
  - 在画笔绘制时实时记录被触碰的像素
  - 笔画结束时只保存实际被编辑的像素到永久掩码
  - 避免将未触碰的像素误标记为已编辑

- **Canvas 组件**：
  - 添加拖放事件处理（`dragEnterEvent`, `dropEvent`）
  - 工具可以随时激活，无需模式切换

- **主窗口**：
  - 移除编辑模式相关逻辑和 UI 元素
  - 简化工具栏布局
  - 添加时间戳文件名生成逻辑
  - 二值化参数变化时只更新基础层，保留编辑内容

### 用户体验提升
- 工作流程更加流畅，无需频繁切换模式
- 可以先画笔编辑，再调整二值化参数，编辑内容完全不受影响
- 可以先调整二值化参数，再进行画笔编辑
- 编辑和参数调整可以任意顺序进行，互不干扰
- 画笔编辑的区域固定不变，未编辑区域随参数实时更新

---

## [v1.0.0.0] - 2026-03-20

### 核心功能

#### 图片处理
- 图片加载和保存（支持 PNG, JPG, BMP 格式）
- 自动转换彩色图片为灰度图
- 图片缩放和平移功能
- 自动适配视图缩放

#### 二值化处理
- **预处理参数**：
  - 曝光度调整 (-100 到 100)
  - 对比度调整 (-100 到 100)
  - 锐化 (0 到 100)
  - 伽马校正 (0.1 到 3.0)
  - 平滑/降噪 (0 到 100)
- **7 种二值化方法**：
  - 固定阈值
  - 自适应阈值（默认）
  - Otsu 自动阈值
  - Sauvola 阈值
  - Wolf 阈值
  - Nick 阈值
  - Bernsen 阈值
- 实时预览所有参数调整效果
- 统一的 `apply_threshold()` 方法支持所有算法

#### 编辑工具
- **画笔工具**：
  - 可调节大小（1-500 像素）
  - 可调节硬度（0.0-1.0）
  - 支持黑色和白色两种颜色
  - 基于间距的点插值算法
  - 实时画笔光标预览
- **裁剪工具**：
  - 左键拖动选择裁剪区域
  - 释放左键立即执行裁剪
  - 保留选中区域，删除其余部分
  - 裁剪后自动重新适配视图
  - 正确计算裁剪矩形（包含起始和结束像素）

#### 历史管理
- 完整的撤销/重做功能
- 使用历史列表和索引指针管理状态
- 支持前后移动（"← 后退" 和 "前进 →"）
- 新编辑操作自动清除重做历史
- 历史记录限制为 50 条

#### 用户界面
- 左右分栏布局（左侧参数面板，右侧编辑区域）
- 工具栏包含所有常用功能
- 预览模式和编辑模式切换
- 模式指示器和切换按钮位于工具栏右侧
- 简洁的界面设计，无冗余元素
- 状态栏显示操作提示

#### 快捷键
- `Ctrl+O` - 打开文件
- `Ctrl+S` - 保存文件
- `Ctrl+Shift+S` - 另存为
- `Ctrl+Z` - 后退（撤销）
- `Ctrl+Y` - 前进（重做）
- `B` - 画笔工具
- `C` - 裁剪工具
- `Space + 拖动` - 平移视图
- `鼠标滚轮` - 缩放视图

### 技术实现

#### 架构设计
- Model-View 架构模式
- 分离的坐标系统（像素坐标 ↔ 视图坐标）
- 临时图层机制（Photoshop 风格）
- 双缓冲渲染避免闪烁

#### 核心类
- `ViewTransform` - 视图变换管理
- `ImageData` - 图片数据模型
- `BrushStroke` - 画笔笔画
- `BrushTool` - 画笔工具
- `CropTool` - 裁剪工具
- `HistoryManager` - 历史管理器
- `BinarizationEngine` - 二值化引擎
- `ImageEnhancer` - 图像增强

#### 测试覆盖
- 9 个单元测试全部通过 ✅
- 测试覆盖核心功能：
  - 坐标转换往返一致性
  - 图片数据像素访问
  - 临时图层机制
  - 二值化算法
  - 画笔笔画光栅化
  - 裁剪功能（包括原地裁剪）
  - 历史管理器撤销/重做

### 技术栈
- **GUI 框架**: PySide6 (Qt 6)
- **图片处理**: NumPy, Pillow, OpenCV
- **测试**: pytest, Hypothesis

### 已知限制
- 历史记录限制为 50 条
- 仅支持二值化图片编辑
- 暂不支持多图层

---

后续版本更新将在此基础上继续迭代。
