# 更新日志

## [v1.0.3.0] - 2026-03-20

### 性能优化
- **分块渲染系统（Tile-based Rendering）**：
  - 将图片分成 256x256 的块，按需生成和缓存
  - 只渲染视口内可见的块，大幅减少渲染开销
  - 平移视图时无需重新生成图片，直接使用缓存块
  - 缩放时只更新可见区域的块
  - LRU 缓存策略，限制内存使用（最多 100 个块）
  - 平移性能提升，缩放性能提升

### 新增功能
- **智能保存逻辑**：
  - 第一次保存（Ctrl+S）：弹出对话框，默认文件名为 `原文件名_时间戳.扩展名`
  - 第二次及之后保存：直接覆盖第一次保存的文件，无需重复选择
  - 另存为（Ctrl+Shift+S）：总是弹出对话框
  - 避免意外覆盖原文件，同时提供快速保存便利

- **工具栏改进**：
  - 所有按钮显示快捷键提示（打开、保存、后退、前进、画笔、裁剪）
  - 右侧显示"当前工具"状态指示器
  - 裁剪工具使用十字光标，画笔工具隐藏光标

### 技术实现
- **TileCache 类**：
  - 分块管理器，处理块的生成、缓存和淘汰
  - 支持多缩放级别的独立缓存
  - 脏区域追踪，只更新受影响的块
  - OrderedDict 实现 LRU 淘汰策略
  - `update_image()` 方法支持增量更新
  - C-contiguous 内存布局确保 QImage 兼容性
  
- **Canvas 渲染优化**：
  - 移除整图缓存，改用分块渲染
  - 视口裁剪，只处理可见区域
  - 懒加载，块在需要时才生成
  - 画笔绘制时实时更新脏区域
  - 精确的坐标计算，减少接缝线
  - 工具专属光标（画笔：隐藏，裁剪：十字）

- **保存逻辑优化**：
  - 使用 `saved_file_path` 跟踪已保存的文件
  - 打开新文件时自动重置保存状态
  - 判断是覆盖还是另存为

### 用户体验提升
- 像素级缩放时平移不再卡顿，丝滑流畅
- 大图片处理更加流畅
- 画笔绘制实时响应，无延迟
- 保存操作更加智能和便捷
- 工具栏信息更加清晰，快捷键一目了然
- 工具光标更加直观

### 已知限制
- 某些缩放级别下可能出现轻微的块接缝线（分块渲染的常见权衡）

---

## [v1.0.2.0] - 2026-03-20

### 新增功能
- **画笔设置悬浮面板**：
  - 鼠标悬停在画笔工具按钮上 0.5 秒显示设置面板
  - 可调节画笔大小（1-500 像素）
  - 可选择颜色（黑色/白色）
  - 快速点击按钮激活工具，悬停显示设置
  - 移出区域自动隐藏

- **智能画笔光标**：
  - 使用画笔工具时隐藏系统光标，只显示圆圈指示器
  - 圆圈小于 30 像素时在外侧显示红色十字准星
  - 十字准星不遮挡圆圈内部，便于精确定位
  - 红色、2 像素粗、8 像素长的十字线

### 性能优化
- **画笔绘制性能大幅提升**：
  - 实现增量光栅化算法，只处理新增的笔画点，避免重复绘制
  - NumPy 向量化批量像素处理，替代 Python 双重循环
  - 综合性能提升使大画笔和像素级缩放下的绘制更加流畅

### 技术改进
- **增量光栅化系统**：
  - `BrushStroke.rasterize()` 支持 `start_index` 参数
  - Canvas 记录已光栅化的点数 (`rasterized_point_count`)
  - 鼠标移动时只处理新增点
  - 避免重复计算和绘制

- **NumPy 向量化优化**：
  - 使用 `np.meshgrid` 创建坐标网格
  - 向量化距离计算和掩码生成
  - 批量设置像素值，替代逐像素循环
  - 同步更新 `temp_layer` 和 `temp_edit_mask`

- **画笔光标优化**：
  - 十字准星分为四段，显示在圆圈外侧
  - 圆圈和十字之间有 2 像素间隙
  - 使用 `Qt.BlankCursor` 隐藏系统光标

- **UI 交互改进**：
  - 画笔按钮使用 QPushButton 支持悬浮事件
  - 延迟显示设置面板避免干扰点击
  - 快捷键 B 正常工作

### 用户体验提升
- 画笔绘制更加流畅，无明显延迟
- 小画笔时十字准星帮助精确定位
- 大画笔时只显示圆圈，视野更清晰
- 设置面板悬浮显示，不占用界面空间

### 问题修复
- 修复了 NumPy 向量化后画笔痕迹消失的问题
- 确保 `temp_edit_mask` 与 `temp_layer` 同步更新
- 画笔笔画现在能正确保存到编辑图层

---

## [v1.0.1.0] - 2026-03-20

### 新增功能
- **拖放支持**：支持将图片文件直接拖放到预览区域加载
  - 支持的格式：PNG, JPG, JPEG, BMP
  - 拖放后自动应用二值化处理

### 重大改进
- **无需切换编辑模式**：
  - 移除了"进入编辑模式"按钮
  - 画笔和裁剪工具可以直接使用，无需切换模式
  - 简化了工作流程，提升用户体验

- **独立编辑图层系统**：
  - 画笔和裁剪操作在独立的编辑图层中进行
  - 使用编辑掩码（mask）存储用户编辑的像素差异
  - **画笔痕迹完全不受二值化参数影响**
  - 只有实际被画笔触碰的像素才会被标记为编辑过的
  - 修改二值化参数时，画笔编辑的内容保持不变
  - 未被编辑的区域会随着二值化参数实时更新
  - 支持在编辑后继续调整二值化参数

- **保存逻辑优化**：
  - 保存文件（Ctrl+S）现在默认为另存为操作
  - 自动生成文件名格式：`原文件名_时间戳.扩展名`
  - 例如：`image.png` → `image_20260320_143025.png`
  - 避免意外覆盖原始图片

### 技术改进
- **图层架构重构**：
  - 基础层（`pixels`）：存储二值化结果，可随时更新
  - 编辑掩码（`edit_mask`）：布尔数组，标记被编辑的像素
  - 编辑值（`edit_values`）：存储用户编辑的像素值
  - 临时层（`temp_layer`）：绘制时的实时预览
  - 临时编辑掩码（`temp_edit_mask`）：记录当前笔画中被触碰的像素
  - 合成显示：基础层 + 编辑掩码覆盖 = 最终显示

- **精确的编辑追踪**：
  - 在画笔绘制时实时记录被触碰的像素
  - 笔画结束时只保存实际被编辑的像素到永久掩码
  - 避免将未触碰的像素误标记为已编辑

- **Canvas 组件**：
  - 添加拖放事件处理（`dragEnterEvent`, `dropEvent`）
  - 工具可以随时激活，无需模式切换

- **主窗口**：
  - 移除编辑模式相关逻辑和 UI 元素
  - 简化工具栏布局
  - 添加时间戳文件名生成逻辑
  - 二值化参数变化时只更新基础层，保留编辑内容

### 用户体验提升
- 工作流程更加流畅，无需频繁切换模式
- 可以先画笔编辑，再调整二值化参数，编辑内容完全不受影响
- 可以先调整二值化参数，再进行画笔编辑
- 编辑和参数调整可以任意顺序进行，互不干扰
- 画笔编辑的区域固定不变，未编辑区域随参数实时更新

---

## [v1.0.0.0] - 2026-03-20

### 核心功能

#### 图片处理
- 图片加载和保存（支持 PNG, JPG, BMP 格式）
- 自动转换彩色图片为灰度图
- 图片缩放和平移功能
- 自动适配视图缩放

#### 二值化处理
- **预处理参数**：
  - 曝光度调整 (-100 到 100)
  - 对比度调整 (-100 到 100)
  - 锐化 (0 到 100)
  - 伽马校正 (0.1 到 3.0)
  - 平滑/降噪 (0 到 100)
- **7 种二值化方法**：
  - 固定阈值
  - 自适应阈值（默认）
  - Otsu 自动阈值
  - Sauvola 阈值
  - Wolf 阈值
  - Nick 阈值
  - Bernsen 阈值
- 实时预览所有参数调整效果
- 统一的 `apply_threshold()` 方法支持所有算法

#### 编辑工具
- **画笔工具**：
  - 可调节大小（1-500 像素）
  - 可调节硬度（0.0-1.0）
  - 支持黑色和白色两种颜色
  - 基于间距的点插值算法
  - 实时画笔光标预览
- **裁剪工具**：
  - 左键拖动选择裁剪区域
  - 释放左键立即执行裁剪
  - 保留选中区域，删除其余部分
  - 裁剪后自动重新适配视图
  - 正确计算裁剪矩形（包含起始和结束像素）

#### 历史管理
- 完整的撤销/重做功能
- 使用历史列表和索引指针管理状态
- 支持前后移动（"← 后退" 和 "前进 →"）
- 新编辑操作自动清除重做历史
- 历史记录限制为 50 条

#### 用户界面
- 左右分栏布局（左侧参数面板，右侧编辑区域）
- 工具栏包含所有常用功能
- 预览模式和编辑模式切换
- 模式指示器和切换按钮位于工具栏右侧
- 简洁的界面设计，无冗余元素
- 状态栏显示操作提示

#### 快捷键
- `Ctrl+O` - 打开文件
- `Ctrl+S` - 保存文件
- `Ctrl+Shift+S` - 另存为
- `Ctrl+Z` - 后退（撤销）
- `Ctrl+Y` - 前进（重做）
- `B` - 画笔工具
- `C` - 裁剪工具
- `Space + 拖动` - 平移视图
- `鼠标滚轮` - 缩放视图

### 技术实现

#### 架构设计
- Model-View 架构模式
- 分离的坐标系统（像素坐标 ↔ 视图坐标）
- 临时图层机制（Photoshop 风格）
- 双缓冲渲染避免闪烁

#### 核心类
- `ViewTransform` - 视图变换管理
- `ImageData` - 图片数据模型
- `BrushStroke` - 画笔笔画
- `BrushTool` - 画笔工具
- `CropTool` - 裁剪工具
- `HistoryManager` - 历史管理器
- `BinarizationEngine` - 二值化引擎
- `ImageEnhancer` - 图像增强

#### 测试覆盖
- 9 个单元测试全部通过 ✅
- 测试覆盖核心功能：
  - 坐标转换往返一致性
  - 图片数据像素访问
  - 临时图层机制
  - 二值化算法
  - 画笔笔画光栅化
  - 裁剪功能（包括原地裁剪）
  - 历史管理器撤销/重做

### 技术栈
- **GUI 框架**: PySide6 (Qt 6)
- **图片处理**: NumPy, Pillow, OpenCV
- **测试**: pytest, Hypothesis

### 已知限制
- 历史记录限制为 50 条
- 仅支持二值化图片编辑
- 暂不支持多图层

---

后续版本更新将在此基础上继续迭代。
